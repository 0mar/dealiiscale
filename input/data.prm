set macro_geometry = [-1,1]x[-1,1]
set macro_rhs = 0
set macro_solution = cos(x0)*cos(x1)*(y0*y0+y1*y1)
set macro_bc = cos(x0)*cos(x1)*(y0*y0+y1*y1)

set lambda = 1.633
set micro_geometry = [-1,1]x[-1,1]
set micro_rhs = 0
set micro_solution = sin(lambda*x) + cos(lambda*y)
set micro_bc = sin(lambda*x) + cos(lambda*y)
